<% if @messages.present? %>
  <% @messages.each do |message| %>
    <div class="row">
      <div class="mess-user">
        <%= message.user.user_name %>
      </div>
      <div class="mess-content">
        <%= message.content %>
      </div>
      </div>
      <br>
  <% end %>
<% end %>
