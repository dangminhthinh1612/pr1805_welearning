$("#<%=buy_icon_id(current_user, @user_course.course)%>").html(
"<%= j(render('user_courses/buy_icon', user: current_user, course: @user_course.course)) %>"
).find('a').first().addClass('btn btn-link');
