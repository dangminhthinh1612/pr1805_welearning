$("#<%=buy_icon_id(current_user, @user_course.course)%>").html(
"<%= j(render('user_courses/buy_icon', user: curre  nt_user, course: @user_course.course)) %>"
).find('a').first().attr('href','<%= @href_path %>').addClass('btn btn-link');
